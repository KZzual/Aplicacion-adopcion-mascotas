# Copia a .env y completa con tu cadena de conexión de Neon (Postgres)
# Formato típico: postgres://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=

# Puerto opcional (por defecto 4000)
PORT=4000
